[build]
builder = "RAILPACK"

[deploy]
startCommand = "flask db upgrade && flask seed-categories && flask seed-budgets && gunicorn app:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
